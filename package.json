{
  "name": "jamie-bradley-dev",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "eleventy && yarn sass",
    "watch": "yarn sass:watch && eleventy --watch",
    "serve": "yarn assets && eleventy --serve",
    "dev": "yarn serve",
    "debug": "DEBUG=* eleventy",
    "sass": "node-sass -r resources/scss/styles.scss -o _assets/css  --output-style compressed",
    "sass:watch": "yarn sass -w",
    "assets": "mkdir -p _site/ && cp -R _assets _site/"
  },
  "keywords": [],
  "author": "Jamie Bradley",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^0.10.0",
    "@sanity/block-content-to-markdown": "^0.0.5",
    "@sanity/client": "^1.149.2",
    "@sanity/image-url": "^0.140.17",
    "dotenv": "^8.2.0",
    "groq": "^1.149.0",
    "markdown-it": "^10.0.0",
    "markdown-it-anchor": "^5.2.5",
    "node-sass": "^4.13.0",
    "sass-mq": "^5.0.1"
  }
}
